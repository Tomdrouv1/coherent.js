---
phase: 03-cli-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/cli/test/scaffold-matrix.test.js
  - packages/cli/test/import-audit.test.js
autonomous: true

must_haves:
  truths:
    - "Tests exist for basic template with all 4 runtimes"
    - "Tests exist for fullstack template with database and auth combinations"
    - "Import audit test validates all generated imports against package exports"
    - "TypeScript scaffold test verifies generated .ts files and valid tsconfig"
  artifacts:
    - path: "packages/cli/test/scaffold-matrix.test.js"
      provides: "Template x runtime x database x auth permutation tests"
      contains: "describe.*scaffold.*matrix"
    - path: "packages/cli/test/import-audit.test.js"
      provides: "Import validation against package exports"
      contains: "@coherent.js"
  key_links:
    - from: "packages/cli/test/scaffold-matrix.test.js"
      to: "packages/cli/src/generators/project-scaffold.js"
      via: "scaffoldProject import"
      pattern: "import.*scaffoldProject"
    - from: "packages/cli/test/import-audit.test.js"
      to: "packages/*/src/index.js"
      via: "package export verification"
      pattern: "@coherent\\.js/"
---

<objective>
Create comprehensive test suite for CLI scaffolding that validates all template permutations and ensures generated imports match actual package exports.

Purpose: Tests prevent future breakage as APIs evolve. The scaffold-matrix tests verify generated projects have correct structure; import-audit tests verify generated code imports from actual package exports.

Output: Two new test files that provide regression coverage for CLI scaffolding.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-scaffolding/03-CONTEXT.md
@.planning/phases/03-cli-scaffolding/03-RESEARCH.md
@.planning/phases/03-cli-scaffolding/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scaffold matrix test suite</name>
  <files>packages/cli/test/scaffold-matrix.test.js</files>
  <action>
Create a new test file that tests scaffold permutations systematically:

1. Set up test utilities:
   - Import vitest (describe, test, expect, beforeAll, afterAll)
   - Import fs/promises (mkdtemp, rm, readFile)
   - Import existsSync from fs
   - Import join from path, tmpdir from os
   - Import scaffoldProject from '../src/generators/project-scaffold.js'
   - Create createTempDir() helper using mkdtemp with 'coherent-matrix-test-' prefix
   - Create cleanupTempDir(dir) helper with recursive removal

2. Create "P0: Basic template x runtime matrix" describe block:
   - Define runtimes array: ['built-in', 'express', 'fastify', 'koa']
   - Loop through runtimes, creating a test for each
   - Each test scaffolds basic template with that runtime, skipInstall: true, skipGit: true
   - Assert structure: src/index.js, src/components/HomePage.js, package.json exist
   - Assert runtime-specific content in index.js:
     - built-in: contains "import http from 'node:http'"
     - express: contains "import express from 'express'" and "setupCoherent"
     - fastify: contains "import Fastify from 'fastify'" and "setupCoherent"
     - koa: contains "import Koa from 'koa'" and "setupCoherent"
   - Assert package.json has @coherent.js/core and runtime-specific deps
   - Always cleanup temp dir in finally block

3. Create "P1: Fullstack template x database matrix" describe block:
   - Define databases array: ['postgres', 'mysql', 'sqlite', 'mongodb']
   - Loop through databases, creating a test for each
   - Each test scaffolds fullstack template with that database, runtime: 'built-in', packages: ['api']
   - Assert database files exist: src/db, src/db/config.js, src/db/index.js, src/db/models/User.js
   - Assert db/index.js contains correct adapter import (PostgreSQLAdapter, MySQLAdapter, etc.)
   - Assert db/index.js contains adapter instantiation with dbConfig
   - Assert package.json has @coherent.js/database

4. Create "P1: Fullstack template x auth matrix" describe block:
   - Define auths: ['jwt', 'session']
   - Define runtimes: ['express', 'fastify', 'koa', 'built-in']
   - Nested loop creating tests for each auth x runtime combination
   - Each test scaffolds fullstack with database: 'sqlite' (required for auth)
   - Assert auth files exist: middleware/auth.js (or plugins/auth.js for fastify), api/auth.js
   - Assert auth routes have correct imports (generateToken, UserModel for JWT)
   - Assert .env.example has JWT_SECRET or SESSION_SECRET

5. Create "TypeScript scaffold" describe block:
   - Test basic TypeScript scaffold: assert .ts files exist, tsconfig.json exists, package.json has typescript and tsx devDeps, tsconfig has correct compilerOptions (module: ESNext)
   - Test fullstack TypeScript scaffold: assert typed model file contains 'interface' and ': Promise<'
  </action>
  <verify>
Run the matrix tests:
```bash
cd packages/cli && pnpm test scaffold-matrix.test.js
```
All tests should pass.
  </verify>
  <done>
- scaffold-matrix.test.js exists with comprehensive permutation tests
- Tests cover basic x 4 runtimes
- Tests cover fullstack x 4 databases
- Tests cover fullstack x 2 auth types x 4 runtimes
- Tests cover TypeScript scaffolding
  </done>
</task>

<task type="auto">
  <name>Task 2: Create import audit test suite</name>
  <files>packages/cli/test/import-audit.test.js</files>
  <action>
Create a test file that validates generated imports against actual package exports:

1. Set up imports and utilities:
   - Import vitest (describe, test, expect)
   - Import fs/promises (mkdtemp, rm, readFile, readdir)
   - Import existsSync from fs
   - Import join from path, tmpdir from os
   - Import scaffoldProject from '../src/generators/project-scaffold.js'

2. Define PACKAGE_EXPORTS constant object mapping package names to their exports:
   - '@coherent.js/core': render, createComponent, defineComponent, registerComponent, withState, createStateManager, memoize, memo, h, createElement, renderWithTemplate, renderComponentFactory, isCoherentComponent
   - '@coherent.js/database': setupDatabase, PostgreSQLAdapter, MySQLAdapter, SQLiteAdapter, MongoDBAdapter, createQuery, executeQuery, createModel, createMigration, createDatabaseManager, withDatabase, withTransaction, withModel, withPagination, createConnection, runMigrations, DEFAULT_DB_CONFIG
   - '@coherent.js/express': setupCoherent, expressEngine
   - '@coherent.js/fastify': coherentFastify, createHandler, setupCoherent
   - '@coherent.js/koa': setupCoherent, coherentKoaMiddleware, createHandler, createKoaIntegration
   - '@coherent.js/api': createRouter, ApiError, ValidationError, AuthenticationError, AuthorizationError, NotFoundError, ConflictError, withErrorHandling, createErrorHandler, validateAgainstSchema, validateField, withValidation, withAuth, withRole, hashPassword, verifyPassword, generateToken
   - '@coherent.js/client': hydrate, EventDelegation, eventDelegation, HandlerRegistry, handlerRegistry, wrapEvent, serializeState, deserializeState, extractState, serializeStateWithWarning, detectMismatch, reportMismatches, formatPath, legacyHydrate, hydrateAll, hydrateBySelector, enableClientEvents, makeHydratable, autoHydrate, registerEventHandler

3. Create extractImports(code) function:
   - Use regex to match named imports: /import\s*\{([^}]+)\}\s*from\s*['"]([^'"]+)['"]/g
   - Parse out individual names (handle 'as' aliases by taking first part)
   - Also match default imports: /import\s+(\w+)\s+from\s*['"]([^'"]+)['"]/g
   - Return array of {names, from, isDefault} objects

4. Create validateImports(imports) function:
   - For each import from a @coherent.js/ package
   - Check if package exists in PACKAGE_EXPORTS
   - Check if each named export exists in that package's exports
   - Return array of error strings

5. Create getAllSourceFiles(dir) async function:
   - Recursively read directory
   - Skip node_modules
   - Collect all .js and .ts files
   - Return array of file paths

6. Create createTempDir() and cleanupTempDir(dir) helpers

7. Create describe block "Import audit: generated code imports from actual exports":
   - Test basic template with built-in runtime
   - Test basic template with express runtime
   - Test fullstack template with database (postgres)
   - Test fullstack template with auth (jwt)
   - Test all database adapters (loop through postgres, mysql, sqlite, mongodb)
   - Each test: scaffold project, get all source files, extract imports, validate, assert no errors

8. Create describe block "Import audit: no undefined exports referenced":
   - Test client package imports in generated code
  </action>
  <verify>
Run the import audit tests:
```bash
cd packages/cli && pnpm test import-audit.test.js
```
All tests should pass.
  </verify>
  <done>
- import-audit.test.js exists with import validation logic
- PACKAGE_EXPORTS maps define known exports from each package
- Tests validate basic, express, fullstack+db, fullstack+auth scaffolds
- Tests provide clear error messages for invalid imports
  </done>
</task>

<task type="auto">
  <name>Task 3: Update existing tests for template consolidation</name>
  <files>packages/cli/test/scaffolding.test.js</files>
  <action>
Update the existing scaffolding tests to reflect the template consolidation:

1. Review the existing test 'scaffoldProject should handle different templates' (around line 252-287):
   - Verify it tests basic template correctly
   - If it tests 'express' as a template, update to test basic template with express runtime instead
   - The test should use template: 'basic', runtime: 'express' not template: 'express'

2. Add a new test to verify consolidated template behavior:
   - Test name: 'scaffoldProject only supports basic and fullstack templates'
   - Create temp directory
   - Test basic template creates src/index.js
   - Test fullstack template with database: null creates src/index.js
   - Test legacy/unsupported template name (e.g., 'custom') still works via fallback behavior
   - Clean up temp directory in finally block

3. Verify all existing tests still pass after template consolidation changes from Plan 01
  </action>
  <verify>
Run all scaffolding tests:
```bash
cd packages/cli && pnpm test scaffolding.test.js
```
All tests should pass.
  </verify>
  <done>
- Existing tests updated for template consolidation
- New test verifies only basic/fullstack templates are primary
- Legacy templates gracefully fall back to defaults
- All scaffolding tests pass
  </done>
</task>

</tasks>

<verification>
1. All matrix tests pass:
   ```bash
   cd packages/cli && pnpm test scaffold-matrix
   ```

2. All import audit tests pass:
   ```bash
   cd packages/cli && pnpm test import-audit
   ```

3. All existing scaffolding tests still pass:
   ```bash
   cd packages/cli && pnpm test scaffolding
   ```

4. Full test suite passes:
   ```bash
   cd packages/cli && pnpm test
   ```
</verification>

<success_criteria>
- scaffold-matrix.test.js exists with 20+ test cases covering permutations
- import-audit.test.js exists with package export validation
- PACKAGE_EXPORTS map correctly lists all @coherent.js package exports
- All tests pass
- Tests provide clear failure messages for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-scaffolding/03-02-SUMMARY.md`
</output>
