name: Cleanup Caches

on:
  schedule:
    - cron: '0 0 * * 0'  # Every Sunday at midnight
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Cleanup
        run: |
          echo "Cache cleanup is handled automatically by GitHub Actions"
          echo "Caches are automatically deleted after 7 days of no access"
